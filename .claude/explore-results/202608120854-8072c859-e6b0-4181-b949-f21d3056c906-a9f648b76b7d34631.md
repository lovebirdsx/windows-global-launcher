# Explore subagent result

- session_id: 8072c859-e6b0-4181-b949-f21d3056c906
- agent_id: a9f648b76b7d34631
- agent_type: Explore
- agent_transcript_path: C:\Users\songxiao\.claude\projects\E--git-project-windows-global-launcher\8072c859-e6b0-4181-b949-f21d3056c906\subagents\agent-a9f648b76b7d34631.jsonl

## Final message

探索完成。以下是详细报告（所有路径为绝对路径）。

## 1. KeyboardHook.cs —— Alt+Q 已实现（当前 HEAD 已包含）

**文件：** `E:\git_project\windows-global-launcher\src\WindowsGlobalLauncher\KeyboardHook.cs`

**重要结论：提交 2ed2f23 "feat: Alt+Q 关闭当前窗口" 已经实现过 Alt+Q，且当前 HEAD (bbf3171) 仍保留该代码。** 该提交只改了 KeyboardHook.cs 一个文件（+18 行），采用**硬编码**方式：在钩子回调里直接判定 `VK_Q + Alt`，然后 `GetForegroundWindow()` + `PostMessage(hwnd, WM_SYSCOMMAND, SC_CLOSE)`（等同 Alt+F4 的效果），并返回 `(IntPtr)1` 吞掉按键。目前**不可配置**。

**结构要点：**

- 类为 `WH_KEYBOARD_LL` 低级键盘钩子，安装在 UI 线程，回调即运行在 UI 线程（类注释，第 7-11 行）。回调只做轻量判定，UI 操作由订阅方用 `Dispatcher.BeginInvoke` 异步执行。
- 回调签名（第 16 行）：`private delegate IntPtr LowLevelKeyboardProc(int nCode, IntPtr wParam, IntPtr lParam);`，实例方法 `HookProc`（第 120 行）。
- 按键判定方式（第 124-130 行）：从 `wParam` 取消息（`WM_KEYDOWN/WM_KEYUP/WM_SYSKEYDOWN/WM_SYSKEYUP`），`Marshal.PtrToStructure<KBDLLHOOKSTRUCT>(lParam)` 取 `vkCode`；修饰键用 `GetAsyncKeyState` 轮询（第 186 行 `IsKeyPressed`）。
- 吞键机制：钩子回调返回 `(IntPtr)1` 即吞掉该键，不再传给系统；否则 `CallNextHookEx`（第 183 行）。
- 事件模型（第 78-94 行）：`AltTab(bool reverse)`、`Commit`、`Cancel`、`Navigate(int)`、`Close`、`MoveMonitor(int)`，全部是 `event Action`。
- 第 97 行：`public Func<bool>? IsSwitcherActive { get; set; }` —— 由 SwitcherWindow 注入，决定是否吞 Esc/触发 Commit/导航。
- 现有 Alt+Q 实现（第 139-145 行）：

```csharp
if (isKeyDown && vk == VK_Q && IsKeyPressed(VK_MENU))
{
    IntPtr hwnd = GetForegroundWindow();
    if (hwnd != IntPtr.Zero)
        PostMessage(hwnd, WM_SYSCOMMAND, (IntPtr)SC_CLOSE, IntPtr.Zero);
    return (IntPtr)1;
}
```

相关常量：`WM_SYSCOMMAND = 0x0112`（第 56 行）、`SC_CLOSE = 0xF060`（第 57 行）、`VK_Q = 0x51`（第 71 行）。

**SwitcherWindow 使用方式**（`E:\git_project\windows-global-launcher\src\WindowsGlobalLauncher\SwitcherWindow.cs`）：
- 第 43 行：`private readonly KeyboardHook _hook = new();`
- 第 65-72 行（构造函数内）：订阅全部事件 + 注入激活判定 + 安装钩子：

```csharp
_hook.AltTab += OnAltTab;
_hook.Commit += OnCommit;
_hook.Cancel += OnCancel;
_hook.Navigate += OnNavigate;
_hook.Close += OnClose;
_hook.MoveMonitor += OnMoveMonitor;
_hook.IsSwitcherActive = () => _isActive;
_hook.Install();
```

- 事件处理都切回 UI 线程，如第 182 行 `private void OnCommit() => Dispatcher.BeginInvoke(Commit);`，第 189-190 行 `OnClose() => Dispatcher.BeginInvoke(() => { if (_isActive) CloseSelected(); });`
- `CloseSelected()`（第 266 行起）调用 `WindowEnumerator.CloseWindow(target.Hwnd)`（第 272 行）。

**注意：** `WindowEnumerator.CloseWindow`（`...\WindowEnumerator.cs` 第 210-215 行）用的是 `SendMessageTimeout(hwnd, WM_CLOSE, ..., SMTO_ABORTIFHUNG)`，与 KeyboardHook 里 Alt+Q 的 `PostMessage(WM_SYSCOMMAND, SC_CLOSE)` 是两种不同的关窗路径，实现可配置 Alt+Q 时可考虑复用/统一。

## 2. AppConfig.cs —— 配置模型

**文件：** `E:\git_project\windows-global-launcher\src\WindowsGlobalLauncher\AppConfig.cs`

- `Config` 类（第 21-26 行）只有三个字段：`MaxDisplayItems`（默认 12）、`HotKey`（默认 `AppConfig.DefaultHotKey`）、`List<ConfigCommand> Commands`。**目前没有"窗口操作类热键"的配置字段**——这是新功能需要扩展的位置。
- `ConfigCommand`（第 10-18 行）：`Name / Description / Shell / HotKey(可空) / RunAsAdmin`。
- `DefaultHotKey = "Ctrl+Shift+I"`（第 30 行）。
- 加载（第 132-162 行 `LoadConfig`）：`JsonSerializer.Deserialize<Config>`，选项 `ReadCommentHandling.Skip + AllowTrailingCommas`（允许带注释 JSON）；成功后触发 `ConfigUpdated` 事件（第 149 行）。文件不存在/出错则 `CreateDefaultConfig()`。
- 单例 `AppConfig.Instance`（第 61-73 行），首次访问即加载。`ConfigUpdated` 事件（第 125 行）由 `LoadConfig` 和文件监视器（`FileSystemWatcher`，第 182-242 行，带 100ms 去抖）触发。
- **SaveConfig（第 255-298 行）确实是手写 JSON 拼接**：先 `JsonSerializer.Serialize` 生成 `json` 变量但**未使用**，然后手工拼 `jsonWithComments` 字符串（第 263-289 行），逐命令用插值拼 `{{ "Name": "{cmd.Name}", ... }}`。注意此手写拼接**不含 JSON 转义**，且新增 Config 字段时必须同步修改这里，否则保存时会丢字段。
- 配置热键的解析在 HotKeyListener（见下）。

## 3. HotKeyListener.cs —— 全局热键

**文件：** `E:\git_project\windows-global-launcher\src\WindowsGlobalLauncher\HotKeyListener.cs`

- 内部用 WinForms `NativeWindow` 建消息窗口（第 31-36 行构造函数），`RegisterHotKey(handle, HOTKEY_ID=9000, modifiers, vk)`，收 `WM_HOTKEY` 后触发 `event Action? HotKeyPressed`（第 29、156-164 行）。
- **热键字符串解析**（第 38-97 行 `RegisterHotKey(string)`）：按 `+` 分割，`ctrl/alt/shift/win`（不区分大小写）映射到 `MOD_*`，其余部分当主键；`GetVirtualKey`（第 120-154 行）支持 SPACE/ENTER/ESC/TAB/F1-F12 及单个 A-Z/0-9 字符。
- 固定单一 `HOTKEY_ID = 9000`（第 20 行）——**目前只能注册一个热键**，要支持多个可配置热键需改为多 id 或改 id 生成方式。

**MainWindow 注册流程**（`E:\git_project\windows-global-launcher\src\WindowsGlobalLauncher\MainWindow.cs`）：
- 第 137 行：`private readonly HotKeyListener _hotKeyListener = new();`
- 第 156 行构造函数调 `SetupHotKeyListener()`（第 461-487 行）：

```csharp
if (!_hotKeyListener.RegisterHotKey(AppConfig.Instance.Config.HotKey))
    _hotKeyListener.RegisterHotKey(AppConfig.DefaultHotKey);   // 失败回退默认

AppConfig.Instance.ConfigUpdated += () =>
{
    Application.Current.Dispatcher.Invoke(() =>                // 必须切回 UI 线程
    {
        _hotKeyListener.UnregisterHotKey();
        var newHotKey = AppConfig.Instance.Config.HotKey;
        if (!_hotKeyListener.RegisterHotKey(newHotKey))
            _hotKeyListener.RegisterHotKey(AppConfig.DefaultHotKey);
    });
};
_hotKeyListener.HotKeyPressed += ShowWindow;
```

这套"ConfigUpdated → Dispatcher.Invoke → 注销重注册"的模式就是新功能可复用的热更新模板。

## 4. App.cs 启动流程

**文件：** `E:\git_project\windows-global-launcher\src\WindowsGlobalLauncher\App.cs`（纯代码启动，无 App.xaml；入口为 `Program.cs`）

`OnStartup`（第 16-32 行）：
1. 确保 `BaseDir = ~\.windows-global-launcher` 存在（第 21-24 行）；
2. `new MainWindow()`（第 27 行）—— 其内部创建 HotKeyListener 并注册主热键；
3. `new SwitcherWindow()` 存入字段 `_switcherWindow`（第 31 行，第 14 行注释明确说明是"保持引用，防止切换器及其键盘钩子被 GC 回收"）—— SwitcherWindow 构造函数内创建并 `Install()` KeyboardHook。

`OnExit`（第 34-38 行）：`_switcherWindow?.Dispose()`，从而卸载键盘钩子。

即 KeyboardHook 的生命周期完全挂在 SwitcherWindow 上；HotKeyListener 挂在 MainWindow 上。

## 5. 测试项目结构

**目录：** `E:\git_project\windows-global-launcher\tests\WindowsGlobalLauncher.Tests\`

- `WindowsGlobalLauncher.Tests.csproj` —— xUnit 测试项目
- `FuzzyMatcherTests.cs` —— `[Theory] + [InlineData]` 风格，直接 new 对象/调静态方法断言，如 `GetMatchScore_WorksAsExpected(query, target, expected)` 用 `Assert.Equal(expected, score, 3)`
- `MainWindowTests.cs` —— 含 `CommandNameWithHotKeyConverter_FormatsCorrectly`（`[Theory]`，第 9-18 行）：`var converter = new CommandNameWithHotKeyConverter();` 然后断言转换输出

测试风格小结：xUnit、`[Fact]`/`[Theory]+[InlineData]`、无 mock 框架、测试纯逻辑（转换器、匹配算法），不碰 UI/Win32。为"可配置 Alt+Q"新增的热键字符串解析逻辑（修饰键+主键 → 修饰键掩码/vkCode）非常适合按此风格抽成纯函数并加 `[Theory]` 测试。

## 对新功能规划的提示

1. Alt+Q 功能已存在但**硬编码在 KeyboardHook.HookProc 第 139-145 行**，不可配置、不可关闭——新功能应把它从硬编码判定重构为配置驱动。
2. 配置侧需扩展 `Config`（如 `CloseWindowHotKey`），并同步修改 `AppConfig.SaveConfig` 的手写 JSON 拼接（第 263-289 行），否则保存丢字段；加载端反序列化自动兼容。
3. 实现路径有两条：(a) 走 KeyboardHook（需要修饰键组合解析 + 配置热更新，可复用 `ConfigUpdated` 模式）；(b) 走 HotKeyListener 的 `RegisterHotKey`（但该类当前固定单 HOTKEY_ID，且 Alt+F4 类按键用 RegisterHotKey 注册 Alt+Q 会全局抢占，无法像 LL 钩子那样精细吞键）。KeyboardHook 的事件表驱动化（把 vk/修饰键 → Action 做成可配置表）更符合"可扩展"诉求。
4. 关窗动作可复用 `WindowEnumerator.CloseWindow`（`SendMessageTimeout + WM_CLOSE`，防卡死），比当前 `PostMessage(WM_SYSCOMMAND, SC_CLOSE)` 更稳。